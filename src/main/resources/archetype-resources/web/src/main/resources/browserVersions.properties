#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
errorMessage=Sirpi  no longer supports your browser, please upgrade. For more information see our FAQs.
acceptedBrowsers=MSIE,Firefox,Chrome,Safari

# New broswers can be added by adding the name of the browser to the list above. This must be the same as the browser name
# 	in the header. The version number and regex properties must have the same name as the browser above.

# Accepted versions for each browser. Must match the name of the browser given above.
version.MSIE=7
version.Firefox=3
version.Chrome=2
version.Safari=4

# Regex used to extract the version number from the user agent header. 
# Must match the name of the browser given above.
# The regex for each browser must have a prefix, the version number and a postfix.
# The browser name should be in either the prefix or the postfix and the version number
# 	must be in the middle section.
regex.Chrome=(.*Chrome/)([0-9]{1,2})(.*)
regex.Firefox=(.*Firefox/)([0-9]{1,2})(.*)
regex.MSIE=(.*MSIE )([0-9]{1,2})(.*)
regex.Safari=(.*Version/)([0-9]{1,2})(.*Safari.*)